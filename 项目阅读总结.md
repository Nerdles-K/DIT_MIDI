# 项目概览：Text-to-MIDI DiT

这是一个 **DDA4220 课程项目**，目标是构建一个**基于扩散 Transformer (DiT) 的文本驱动 MIDI 音乐生成系统**。给一句自然语言（如 `"happy piano melody in C major"`），生成对应风格的 `.mid` 文件。

---

## 一、你做了什么（按时间线整理）

从 [README.md](README.md) 和 [docs/](docs/) 的实验报告看，你迭代了 **5 个版本**：

| 版本 | 优化器 | 模型规模 | 数据增强 | 最佳 Val Loss | 备注 |
|------|--------|----------|----------|---------------|------|
| v3.0 | AdamW | 125M | ❌ | — | 初始版本 |
| **v3.1** | **AdamW** | **125M** | ❌ | **70.95** ✅ | 历史最佳 |
| v4.2 | Muon | 125M | ❌ | 101.19 | Muon 实验失败 |
| v5.0 | Muon | 75M | ✅ medium | 171.54 | 模型缩小+增强，效果反而更差 |
| v5.1 | AdamW | 75M | ✅ light | 进行中 | 回到 AdamW + 弱增强 |

主要做的工作：

1. **核心架构搭建** — DiT + AdaLN-Zero + 文本条件 ([main/midi_dit.py](main/midi_dit.py))
2. **数据管道** — MIDI → Piano Roll → Patch 化 + 自动文本特征提取 ([main/data/dataset_midi.py](main/data/dataset_midi.py))
3. **训练框架** — PyTorch Lightning + Hydra + WandB ([main/module_midi_dit.py](main/module_midi_dit.py), [train_midi.py](train_midi.py))
4. **优化器实验** — 自己实现了 Muon (Newton-Schulz 正交化) ([main/muon_optimizer.py](main/muon_optimizer.py))，结果不如 AdamW
5. **数据增强** — 6 种 MIDI 增强（音高移位/时间拉伸/力度缩放/音符 dropout 等）([main/data/data_augmentation.py](main/data/data_augmentation.py))
6. **推理与评估** — DDPM 采样 + 客观指标 ([scripts/inference_text_to_midi.py](scripts/inference_text_to_midi.py), [scripts/run_evaluation.py](scripts/run_evaluation.py))
7. **Baseline 对比** — 针对 Discrete Diffusion / GetMusic / MuseCoco 写了对照报告 ([baseline_docs/](baseline_docs/))
8. **大量分析脚本** — wandb 曲线绘图、参数/数据比分析、消融研究等 ([utils/](utils/), [scripts/aggregate_ablation_results.py](scripts/aggregate_ablation_results.py))

实验跑了 **30+ 次** 训练（看 [logs/wandb/](logs/wandb/) 下的 offline-run 数量）。

---

## 二、整体模型架构

### 数据流（端到端）

```
  ┌─ MIDI 文件 ─┐
  │             │
  ▼             ▼
特征提取        Piano Roll 矩阵 (T × 88)
(乐器/调性/        │
 速度/情绪)        ▼
  │           Patchify 16×16
  ▼               │
文本描述           ▼
"piano happy   Patches (N × 256)
 melody in C"     │
  │               │
  ▼               ▼
[训练时]    加噪 noisy = clean + σ·ε
  │               │
  └────► DiT ◄────┘
            │
            ▼
        预测噪声 ε̂  →  MSE Loss
```

### 核心模型 `MIDIDiT` ([main/midi_dit.py:246](main/midi_dit.py#L246))

一个 **三路条件**的 Diffusion Transformer：

```
        ┌─ Timestep t ──► Fourier + MLP ──► t_emb (768)
        │
条件 c =─┤── Class label ──► Embedding ────► c_emb (768)
        │
        └─ Text "piano..." ──► TextEncoder ──► text_emb (768)
                                                    │
                            cond = concat ──────► (B, 2304)

  Noisy Patches (B, N, 256)
        │
        ▼
   PatchEmbed (Linear 256→768)
        │
        ▼
   + Positional Embedding (learnable)
        │
        ▼
   ┌─────── DiTBlock × 12 ───────┐
   │   AdaLN-Zero(x, cond)       │
   │   ──► MultiHead Self-Attn   │
   │   ──► gate_attn · residual  │ ← gate 由 cond 生成，零初始化
   │                              │
   │   AdaLN-Zero(x, cond)       │
   │   ──► MLP (4×)              │
   │   ──► gate_mlp · residual   │
   └──────────────────────────────┘
        │
        ▼
   Final AdaLN-Zero(x, cond)
        │
        ▼
   Linear 768→256 (零初始化)
        │
        ▼
   预测的噪声 (B, N, 256)
```

### 关键组件

**1. AdaLNZero** ([main/midi_dit.py:22](main/midi_dit.py#L22))
- 公式：`(1 + γ(c)) · LN(x) + β(c)`，γ/β 由条件 c 通过 MLP 生成
- **零初始化**：训练初期相当于恒等映射，让模型从"什么都不做"平稳启动
- 论文：Peebles & Xie, DiT (2023)

**2. SimpleTextEncoder** ([main/text_encoder.py:13](main/text_encoder.py#L13))
- **自定义词表 56 词**（乐器 14 种 + 情绪 4 种 + 调性 12 种 + 速度 8 种 + 强度 + 描述词）
- 4 层 Transformer Encoder + `[CLS]` token 池化
- 没用 CLIP/BERT，因为词表受控、轻量

**3. MIDIPatchify** ([main/data/dataset_midi.py:46](main/data/dataset_midi.py#L46))
- 把 Piano Roll `(T, 88)` 切成 `16×16` 的 patch，展平成 `(N, 256)` 序列
- 类似 ViT 把图像切成 patch 输入 Transformer

**4. MusicFeatureExtractor** ([main/data/dataset_midi.py:117](main/data/dataset_midi.py#L117))
- 自动从 MIDI 文件提取乐器、tempo、调性（Krumhansl-Schmuckler 算法）、力度
- 拼成文本：`"{乐器} {强度} {情绪} melody in {调性}"`
- 这样**无需人工标注**就能构造 (text, MIDI) 配对

**5. 扩散过程** ([main/module_midi_dit.py:29](main/module_midi_dit.py#L29))
- EDM 风格的 σ 调度：`σ_min=0.002, σ_max=80, ρ=7`
- 训练目标：`MSE(model(x + σε, σ, c), ε)`
- 采样：DDPM 50 步迭代去噪

---

## 三、当前配置（v5.1，[exp/train_babyslakh_midi_dit.yaml](exp/train_babyslakh_midi_dit.yaml)）

| 项 | 值 |
|---|---|
| 数据集 | Lakh MIDI Dataset (lmd_full)，500 文件 |
| Patch | 16×16 → patch_dim=256 |
| 模型 | embed_dim=512, depth=8, heads=8, ~75M 参数 |
| 优化器 | AdamW，lr=6e-5，weight_decay=0.03 |
| Warmup | 2000 steps，之后 cosine annealing |
| Batch | 16，FP16，gradient_clip=0.5 |
| Epoch | 50，每 2 epoch 验证一次，patience=12 |
| 增强 | light 强度 |

---

## 四、值得注意的发现

1. **Muon 优化器对你这个任务并不好用** — 你专门写了 [docs/附录_Muon优化器改进记录.md](docs/附录_Muon优化器改进记录.md) 记录这件事。v3.1 的 AdamW (val=70.95) > v4.2 Muon (101.19)。
2. **小模型 + 强增强反而更差** — v5.0 把模型从 125M 缩到 75M 又开了 medium 增强，val 飙到 171.54。v5.1 回退到 light 增强是明智的。
3. **Pitch KL 已经显著优于多个 Baseline** — 1.60 nats 比 Diff Baseline 的 5.16 和 GetMusic 的 12.28 都好得多 ([experiment_results/EXPERIMENT_SUMMARY.md](experiment_results/EXPERIMENT_SUMMARY.md))。
4. **Empty Bar Rate (13.6%) 和 Duration Entropy (0.06) 还是弱项** — 生成偏稀疏、节奏单一，是后续可以重点改的方向。
